# v0.3 - Testing Skill + Contact Popup + Manager Restore Guard

## Completed Changes

- Added global Codex skill `procurement-testing-standard-op` for the project testing workflow.
- Added bilingual testing SOP files:
  - `_doc/testing-standard-op.en.md`
  - `_doc/testing-standard-op.zh-TW.md`
- Removed `Contacts` as a top-level navigation tab.
- Replaced the former Contact Directory page with a compact topbar Contact popup.
- Kept row-level `Contact DRI` modal for Manager approval support.
- Restored Manager B's official structure:
  - `Approval`
  - `Demand Analysis`
  - `Progress Tracking`
  - `Project Setup`
- Kept `Demand Analysis` as a two-layer workspace:
  - `Cost Dashboard`
  - `Station Matrix`

## Test Contract Updates

- System contract tests now verify that `Contacts` is not a top-level view.
- System contract tests verify the topbar Contact popup trigger and modal exist.
- System contract tests continue to guard Manager B and OM Purchasing tab contracts.

## Standard Test Rule

Every major UI or workflow change must run:

```bash
./test.sh
```

Browser smoke may be skipped only when Playwright is unavailable, and the skip must be explicitly reported.

## Remaining Engineering Debt

- `app.js` remains oversized and should continue to be split into modules.
- Contact popup still reads from in-page data helpers; a future module split should move contact aggregation into a dedicated shared module.
